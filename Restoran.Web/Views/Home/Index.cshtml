@{
    ViewData["Title"] = "Home Page";
}

<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Modern Home Page</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet">
    <style>
        html, body {
            margin: 0;
            padding: 0;
            font-family: 'Segoe UI', sans-serif;
            background-color: #f4f4f4;
        }

        .carousel-item {
            position: relative;
            height: 100vh;
        }

            .carousel-item img {
                object-fit: cover;
                width: 100%;
                height: 100%;
                filter: brightness(60%);
            }

        .carousel-caption {
            position: absolute;
            top: 50%;
            transform: translateY(-50%);
            text-align: left;
            left: 10%;
            color: white;
        }

            .carousel-caption h1 {
                font-size: 3rem;
                font-weight: bold;
            }

            .carousel-caption p {
                font-size: 1.2rem;
                max-width: 600px;
            }

        .intro-section {
            padding: 80px 20px;
            background-color: white;
        }

            .intro-section img {
                border-radius: 15px;
                box-shadow: 0 8px 24px rgba(0, 0, 0, 0.1);
            }

        .certificates {
            padding: 60px 0;
            background-color: #f8f9fa;
        }

        .cert-card {
            background-color: white;
            border-radius: 12px;
            padding: 30px 20px;
            box-shadow: 0 4px 16px rgba(0, 0, 0, 0.07);
            transition: transform 0.2s ease;
        }

            .cert-card:hover {
                transform: translateY(-5px);
            }

        .cert-icon {
            font-size: 2.5rem;
            color: #198754;
            margin-bottom: 10px;
        }

        .cert-text {
            font-size: 0.95rem;
            color: #555;
        }
    </style>
</head>
<body>

    <!-- Fullscreen Slider -->
    <div id="heroCarousel" class="carousel slide" data-bs-ride="carousel">
        <div class="carousel-inner">
            <div class="carousel-item active">
                <img src="https://picsum.photos/1920/1080?grayscale&random=1" alt="Slide 1">
                <div class="carousel-caption">
                    <h1>Lezzetin Yeni Adresi</h1>
                    <p>Modern sunumlarla geleneksel tatların buluştuğu restoranımıza hoş geldiniz.</p>
                    <a href="#about" class="btn btn-success btn-lg mt-3">Keşfet</a>
                </div>
            </div>
            <div class="carousel-item">
                <img src="https://picsum.photos/1920/1080?grayscale&random=2" alt="Slide 2">
                <div class="carousel-caption">
                    <h1>Tazelik & Kalite</h1>
                    <p>En taze malzemelerle hazırlanan eşsiz lezzetler burada hayat buluyor.</p>
                    <a href="#about" class="btn btn-success btn-lg mt-3">Menüyü Gör</a>
                </div>
            </div>
        </div>
        <button class="carousel-control-prev" type="button" data-bs-target="#heroCarousel" data-bs-slide="prev">
            <span class="carousel-control-prev-icon"></span>
            <span class="visually-hidden">Önceki</span>
        </button>
        <button class="carousel-control-next" type="button" data-bs-target="#heroCarousel" data-bs-slide="next">
            <span class="carousel-control-next-icon"></span>
            <span class="visually-hidden">Sonraki</span>
        </button>
    </div>

    <!-- Intro Section -->
    <section class="intro-section" id="about">
        <div class="container">
            <div class="row align-items-center">
                <div class="col-lg-6 mb-4">
                    <img src="https://picsum.photos/600/400?food" alt="Yemek" class="img-fluid">
                </div>
                <div class="col-lg-6">
                    <h2 class="mb-4">Neden Biz?</h2>
                    <p class="text-muted">
                        Usta şeflerimizin ellerinden çıkan her tabak, hem göze hem damağa hitap ediyor.
                        Restoranımızda tazelik, hijyen ve misafir memnuniyeti en ön planda tutulur.
                    </p>
                    <p class="text-muted">
                        Sadece yemek değil, unutulmaz bir deneyim sunmak için buradayız.
                    </p>
                    <a href="#" class="btn btn-outline-success mt-3">Daha Fazla</a>
                </div>
            </div>
        </div>
    </section>

    <!-- Certificates Section -->
    <section class="certificates">
        <div class="container">
            <div class="text-center mb-5">
                <h3 class="fw-bold">Kalite Sertifikalarımız</h3>
                <p class="text-muted">Uluslararası kalite standartlarıyla belgelenmiş bir mutfağa sahibiz.</p>
            </div>
            <div class="row g-4 justify-content-center">
                <div class="col-md-4 col-sm-6">
                    <div class="cert-card text-center">
                        <div class="cert-icon"><i class="bi bi-award-fill"></i></div>
                        <div class="fw-semibold">ISO 9001: Kalite Yönetimi</div>
                        <div class="cert-text">Mükemmeliyetçi yönetim anlayışımız belgelendi.</div>
                    </div>
                </div>
                <div class="col-md-4 col-sm-6">
                    <div class="cert-card text-center">
                        <div class="cert-icon"><i class="bi bi-shield-lock-fill"></i></div>
                        <div class="fw-semibold">Gıda Güvenliği</div>
                        <div class="cert-text">Tüm süreçlerimiz güvenlik standartlarına uygun.</div>
                    </div>
                </div>
                <div class="col-md-4 col-sm-6">
                    <div class="cert-card text-center">
                        <div class="cert-icon"><i class="bi bi-heart-pulse-fill"></i></div>
                        <div class="fw-semibold">Hijyen Belgesi</div>
                        <div class="cert-text">Yüksek temizlik standartları ile üretim yapılıra.</div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>

